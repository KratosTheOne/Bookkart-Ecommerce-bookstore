'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-b93ab635.js');
const icons = require('./icons-0bc4ad6b.js');

const amplifyIconCss = ".sc-amplify-icon-h{--icon-fill-color:var(--amplify-white)}.icon.sc-amplify-icon{fill:var(--icon-fill-color)}";

class AmplifyIcon {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
    }
    validateName(newValue) {
        const isBlank = typeof newValue == null;
        if (isBlank) {
            throw new Error('name: required');
        }
    }
    // https://stenciljs.com/docs/templating-jsx#avoid-shared-jsx-nodes
    render() {
        return index.h("span", { class: "icon" }, icons.icons[this.name]());
    }
    static get watchers() { return {
        "name": ["validateName"]
    }; }
}
AmplifyIcon.style = amplifyIconCss;

exports.amplify_icon = AmplifyIcon;
